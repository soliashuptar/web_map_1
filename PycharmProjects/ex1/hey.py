import math

def square(a, b, c, num):
    if a == b:
        long = c
        short = a
    elif a == c:
        long = b
        short = a
    else:
        long = a
        short = b
    helper = long / 2
    full = helper * math.sqrt((short + helper) * (short - helper))
    part1 = x * ((long - x) / 2)
    part2 =
    while result - whole > num:
